version: "3.8"

services:
  ib-gateway:
    image: ghcr.io/gnzsnz/ib-gateway:stable
    container_name: ib-gateway
    restart: unless-stopped
    ports:
      - "4001:4001"   # Paper trading API
      - "4002:4002"   # Live trading API (not used yet)
      - "5900:5900"   # VNC for initial setup
    environment:
      # IBKR Credentials (set via .env file)
      - TWS_USERID=${TWS_USERID}
      - TWS_PASSWORD=${TWS_PASSWORD}
      - TWS_TOTP_SECRET=${TWS_TOTP_SECRET:-}
      # Trading mode: paper or live
      - TRADING_MODE=paper
      # Accept incoming API connections
      - TWS_ACCEPT_INCOMING=accept
      # Read-only mode (set to 'no' for actual trading)
      - READ_ONLY_API=no
      # VNC password for remote access
      - VNC_SERVER_PASSWORD=${VNC_PASSWORD:-ibgateway}
      # Timezone
      - TZ=Europe/Paris
    volumes:
      # Persist IBC config and TWS settings
      - ib-gateway-ibc:/root/ibc
      - ib-gateway-tws:/root/Jts
    healthcheck:
      # "healthy" only when TWS API socket accepts handshake (not just container Up)
      test:
        [
          "CMD-SHELL",
          "printf 'API\\0v100..151\\0' | timeout 5 nc 127.0.0.1 4002 | head -c 1 >/dev/null"
        ]
      interval: 20s
      timeout: 10s
      retries: 6
      start_period: 90s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  ib-gateway-ibc:
  ib-gateway-tws:
